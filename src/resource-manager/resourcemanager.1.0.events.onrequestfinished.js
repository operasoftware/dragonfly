// Autogenerated by hob
window.cls || (window.cls = {});
cls.ResourceManager || (cls.ResourceManager = {});
cls.ResourceManager["1.0"] || (cls.ResourceManager["1.0"] = {});

cls.ResourceManager["1.0"].RequestFinished = function(arr)
{
  this.requestID = arr[0];
  this.resourceID = arr[1];
  this.time = arr[2];
  this.data = arr[3] ? new cls.ResourceManager["1.0"].RequestData(arr[3]) : null;
};

cls.ResourceManager["1.0"].RequestData = function(arr)
{
  this.requestID = arr[0];
  this.mimeType = arr[1];
  this.characterEncoding = arr[2];
  this.contentLength = arr[3];
  this.content = arr[4] ? new cls.ResourceManager["1.0"].Content(arr[4]) : null;
  /** 
    * If the payload is multipart, this list contains the
    * individual parts.
    */
  this.partList = (arr[5] || []).map(function(item)
  {
    return new cls.ResourceManager["1.0"].Part(item);
  });
};

cls.ResourceManager["1.0"].Content = function(arr)
{
  this.length = arr[0];
  this.byteData = arr[1];
  this.stringData = arr[2];
};

cls.ResourceManager["1.0"].Part = function(arr)
{
  this.headerList = (arr[0] || []).map(function(item)
  {
    return new cls.ResourceManager["1.0"].Header(item);
  });
  this.contentLength = arr[1];
  this.content = arr[2] ? new cls.ResourceManager["1.0"].Content(arr[2]) : null;
};

cls.ResourceManager["1.0"].Header = function(arr)
{
  this.name = arr[0];
  this.value = arr[1];
};

