message <span class="message">MissingTrail</span>
{
  message <span class="message">Inner</span>
  {
    required <span class="string">string</span> a = 1; 
  }
  optional <span class="number">int32 </span> a     = 1; 
  optional <span class="string">string</span> b     = 2; 
  optional <span class="bytes">bytes </span> c     = 3; 
  optional <span class="message">Inner </span> d     = 4; 
  repeated <span class="number">int32 </span> eList = 5; 
  repeated <span class="string">string</span> fList = 6; 
  repeated <span class="bytes">bytes </span> gList = 7; 
  repeated <span class="message">Inner </span> hList = 8; 
}
