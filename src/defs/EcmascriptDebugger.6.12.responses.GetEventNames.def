<span class="comment">/** 
 * Contains a list of unique event-names found in the DOM document.
 * 
 * @since 6.11
 */</span>
message <span class="message">EventNames</span>
{
  <span class="comment">/** 
   * The runtime ID where the inspection was performed.
   */</span>
  required <span class="number">uint32</span> runtimeID     = 1; 
  <span class="comment">/** 
   * ID of the root node which was used as a starting point.
   */</span>
  required <span class="number">uint32</span> objectID      = 2; 
  <span class="comment">/** 
   * The unique list of event-names which were found in the document.
   * e.g. ["click", "load", "blur"]
   */</span>
  repeated <span class="string">string</span> eventNameList = 3; 
}
