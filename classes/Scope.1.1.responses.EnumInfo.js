// Autogenerated by hob
window.cls || (window.cls = {});
cls.Scope || (cls.Scope = {});
cls.Scope["1.1"] || (cls.Scope["1.1"] = {});

/** 
  * Contains a list of enums.
  * @since 1.1
  */
cls.Scope["1.1"].EnumList = function(arr)
{
  this.enumList = (arr[0] || []).map(function(item)
  {
    return new cls.Scope["1.1"].Enum(item);
  });
};

/** 
  * Introspection result for a given enum.
  * 
  * An enum is defined as::
  *   enum Type
  *   {
  *      TYPE1 = 1;
  *      TYPE2 = 2;
  *   }
  * @since 1.1
  */
cls.Scope["1.1"].Enum = function(arr)
{
  /** 
    * Unique ID for this enum.
    */
  this.id = arr[0];
  this.name = arr[1];
  this.valueList = (arr[2] || []).map(function(item)
  {
    return new cls.Scope["1.1"].Value(item);
  });
};

/** 
  * Represents a value in an enum.
  * e.g. TYPE1 = 1;
  * TYPE1 would then be the name and 1 the number.
  */
cls.Scope["1.1"].Value = function(arr)
{
  this.name = arr[0];
  this.number = arr[1];
};

