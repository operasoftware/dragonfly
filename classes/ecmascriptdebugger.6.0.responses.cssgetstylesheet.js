// Autogenerated by hob
window.cls || (window.cls = {});
cls.EcmascriptDebugger || (cls.EcmascriptDebugger = {});
cls.EcmascriptDebugger["6.0"] || (cls.EcmascriptDebugger["6.0"] = {});

cls.EcmascriptDebugger["6.0"].CssStylesheetRules = function(arr)
{
  this.ruleList = (arr[0] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].StylesheetRule(item);
  });
};

/** 
  * Common to FONT_FACE, PAGE and STYLE is:
  * - indexList
  * - valueList
  * - priorityList
  * 
  * Common to STYLE and PAGE is:
  * - selectorList
  * - specificityList
  * 
  * Common to MEDIA and IMPORT
  * - mediaList
  * 
  * For MEDIA
  * - ruleList
  * 
  * For IMPORT
  * - href
  * - importStylesheetID
  * 
  * For PAGE
  * - pseudoClass
  * 
  * For CHARSET
  * - charset
  */
cls.EcmascriptDebugger["6.0"].StylesheetRule = function(arr)
{
  /** 
    * Type values:
    * 0 - UNKNOWN
    * 1 - STYLE
    * 2 - CHARSET
    * 3 - IMPORT
    * 4 - MEDIA
    * 5 - FONT_FACE
    * 6 - PAGE
    * 7 - NAMESPACE // Not supported
    */
  this.type = arr[0];
  this.stylesheetID = arr[1];
  this.ruleID = arr[2];
  this.indexList = (arr[3] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].uint32(item);
  });
  this.valueList = (arr[4] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].string(item);
  });
  this.priorityList = (arr[5] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].bool(item);
  });
  /** 
    * 0..1 for PAGE and 0.. for STYLE
    */
  this.selectorList = (arr[6] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].string(item);
  });
  /** 
    * 1..1 for PAGE and 0.. for STYLE
    */
  this.specificityList = (arr[7] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].uint32(item);
  });
  this.mediaList = (arr[8] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].string(item);
  });
  this.ruleList = (arr[9] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].CssStylesheetRules.StylesheetRule(item);
  });
  this.href = arr[10];
  this.importStylesheetID = arr[11];
  this.pseudoClass = arr[12];
  this.charset = arr[13];
};

