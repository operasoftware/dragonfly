// Autogenerated by hob
window.cls || (window.cls = {});
cls.EcmascriptDebugger || (cls.EcmascriptDebugger = {});
cls.EcmascriptDebugger["6.0"] || (cls.EcmascriptDebugger["6.0"] = {});

cls.EcmascriptDebugger["6.0"].NodeList = function(arr)
{
  this.nodeList = (arr[0] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].NodeInfo(item);
  });
};

cls.EcmascriptDebugger["6.0"].NodeInfo = function(arr)
{
  this.objectID = arr[0];
  this.type = arr[1];
  this.name = arr[2];
  this.depth = arr[3];
  /** 
    * Used if type is 1.
    */
  this.namespacePrefix = arr[4];
  /** 
    * Used if type is 1.
    */
  this.attributeList = (arr[5] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].Attribute(item);
  });
  /** 
    * Used if type is 1.
    */
  this.childrenLength = arr[6];
  /** 
    * Used if type is 3, 4, 7 or 8.
    */
  this.value = arr[7];
  /** 
    * Used if type is 10.
    */
  this.publicID = arr[8];
  /** 
    * Used if type is 10.
    */
  this.systemID = arr[9];
};

cls.EcmascriptDebugger["6.0"].Attribute = function(arr)
{
  this.namePrefix = arr[0];
  this.name = arr[1];
  this.value = arr[2];
};

