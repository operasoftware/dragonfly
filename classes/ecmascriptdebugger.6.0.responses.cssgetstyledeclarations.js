// Autogenerated by hob
window.cls || (window.cls = {});
cls.EcmascriptDebugger || (cls.EcmascriptDebugger = {});
cls.EcmascriptDebugger["6.0"] || (cls.EcmascriptDebugger["6.0"] = {});

cls.EcmascriptDebugger["6.0"].CssStyleDeclarations = function(arr)
{
  this.computedStyleList = (arr[0] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].string(item);
  });
  this.nodeStyleList = (arr[1] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].NodeStyle(item);
  });
};

cls.EcmascriptDebugger["6.0"].NodeStyle = function(arr)
{
  this.objectID = arr[0];
  this.elementName = arr[1];
  this.styleList = (arr[2] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].StyleDeclaration(item);
  });
};

/** 
  * Common to all origins:
  * - indexList
  * - valueList
  * - priorityList
  * - statusList
  * 
  * Common to AUTHOR and LOCAL
  * - selector
  * - specificity
  * 
  * For AUTHOR
  * - stylesheetID
  * - ruleID
  * - ruleType
  */
cls.EcmascriptDebugger["6.0"].StyleDeclaration = function(arr)
{
  /** 
    * 1 = USER-AGENT, 2=LOCAL, 3=AUTHOR, 4=ELEMENT
    */
  this.origin = arr[0];
  this.indexList = (arr[1] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].uint32(item);
  });
  this.valueList = (arr[2] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].string(item);
  });
  this.priorityList = (arr[3] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].bool(item);
  });
  this.statusList = (arr[4] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].uint32(item);
  });
  this.selector = arr[5];
  this.specificity = arr[6];
  this.stylesheetID = arr[7];
  this.ruleID = arr[8];
  this.ruleType = arr[9];
};

