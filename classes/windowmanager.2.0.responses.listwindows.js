// Autogenerated by hob
window.cls || (window.cls = {});
cls.WindowManager || (cls.WindowManager = {});
cls.WindowManager["2.0"] || (cls.WindowManager["2.0"] = {});

cls.WindowManager["2.0"].WindowList = function(arr)
{
  this.windowList = (arr[0] || []).map(function(item)
  {
    return new cls.WindowManager["2.0"].WindowInfo(item);
  });
};

/** 
  * TODO: We probably want a lot more information about each window, like URL, shortcut
  *       icon, etc.
  */
cls.WindowManager["2.0"].WindowInfo = function(arr)
{
  this.windowID = arr[0];
  this.title = arr[1];
  /** 
    * 
    * TODO: Some of these are obsolete, like "ad". Figure out which ones should really be present.
    * 
    * Type is one of:
    * - "normal"
    * - "download"
    * - "cache"
    * - "plugins"
    * - "history"
    * - "help"
    * - "mail_view"
    * - "mail_compose"
    * - "newsfeed_view"
    * - "ad"
    * - "im_view"
    * - "p2p_view"
    * - "brand_view"
    * - "print_selection"
    * - "js_console"
    * - "gadget"
    * - "controller"
    * - "info"
    * - "dialog"
    * - "thumbnail"
    */
  this.windowType = arr[2];
  /** 
    * The `window-id` of the window who opened the window in question,
    * or 0 if it was not opened by another window.
    */
  this.openerID = arr[3];
};

