// Autogenerated by hob
window.cls || (window.cls = {});
cls.Exec || (cls.Exec = {});
cls.Exec["2.0"] || (cls.Exec["2.0"] = {});

cls.Exec["2.0"].ScreenWatcherResult = function(arr)
{
  /** 
    * The ID of the window that was triggered by a screen watch, or 0 if the screen watch failed or was cancelled
    */
  this.windowID = arr[0];
  /** 
    * ``MD5`` sum of an image, in hexadecimal.
    */
  this.md5 = arr[1];
  this.png = arr[2];
  this.colorMatchList = (arr[3] || []).map(function(item)
  {
    return new cls.Exec["2.0"].ColorMatch(item);
  });
};

cls.Exec["2.0"].ColorMatch = function(arr)
{
  /** 
    * The `ColorSpec.id` which matched a color
    */
  this.id = arr[0];
  /** 
    * Number of pixels for the matching `id`
    */
  this.count = arr[1];
};

