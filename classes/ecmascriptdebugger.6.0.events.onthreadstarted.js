// Autogenerated by hob
window.cls || (window.cls = {});
cls.EcmascriptDebugger || (cls.EcmascriptDebugger = {});
cls.EcmascriptDebugger["6.0"] || (cls.EcmascriptDebugger["6.0"] = {});

cls.EcmascriptDebugger["6.0"].ThreadInfo = function(arr)
{
  this.runtimeID = arr[0];
  this.threadID = arr[1];
  this.parentThreadID = arr[2];
  /** 
    * One of:
    * - `"inline"`
    * - `"event"`
    * - `"linked"`
    * - `"timeout"`
    * - `"java"`
    * - `"unknown"`
    */
  this.threadType = arr[3];
  /** 
    * present if `threadType` is `"event"`
    */
  this.eventNamespace = arr[4];
  /** 
    * present if `threadType` is `"event"`
    */
  this.eventType = arr[5];
};

