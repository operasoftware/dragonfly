// Autogenerated by hob
window.cls || (window.cls = {});
cls.EcmascriptDebugger || (cls.EcmascriptDebugger = {});
cls.EcmascriptDebugger["6.0"] || (cls.EcmascriptDebugger["6.0"] = {});

cls.EcmascriptDebugger["6.0"].CssStylesheetList = function(arr)
{
  this.stylesheetList = (arr[0] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].Stylesheet(item);
  });
};

cls.EcmascriptDebugger["6.0"].Stylesheet = function(arr)
{
  this.objectID = arr[0];
  this.isDisabled = arr[1];
  this.href = arr[2];
  this.title = arr[3];
  this.type = arr[4];
  this.mediaList = (arr[5] || []).map(function(item)
  {
    return new cls.EcmascriptDebugger["6.0"].string(item);
  });
  this.ownerNodeID = arr[6];
  this.ownerRuleID = arr[7];
  this.parentStylesheetID = arr[8];
};

